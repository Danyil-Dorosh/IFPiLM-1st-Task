Title: Switch to open-folder task
Branch: adaptive-point-selection
Context: need to implement "open folder of data" UI/func to inspect other datasets.
Paths:
  - data/other/  -> contains multiple txt files (see data/other/*.txt)
Notes / next steps:
  - implement folder picker that lists files under data/
  - add preview of first N frames for selected file
  - check threshold behavior across files; compare bg/scale values
  - remember: current fit work is in `adaptive point selection.py`
EOF