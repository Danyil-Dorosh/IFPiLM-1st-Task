---
description: Modular code reuse and deduplication
applyTo: "**"
---

- Reuse existing project functions before writing new ones. Do not reimplement existing logic.
- If code is repeated, extract helper functions instead of duplicating blocks.
- Oriante and write functions in a way they can be reused across different parts of the project